% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Predict m6A Status for a Single Site}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{A pre-trained model object (e.g., of class `randomForest`).}

\item{gc_content}{Numeric value for the GC content.}

\item{RNA_type}{Character string for the RNA type (e.g., "mRNA").}

\item{RNA_region}{Character string for the RNA region (e.g., "CDS").}

\item{exon_length}{Numeric value for the exon length.}

\item{distance_to_junction}{Numeric value for the distance to a splice junction.}

\item{evolutionary_conservation}{Numeric value for the conservation score.}

\item{DNA_5mer}{A 5-character string for the DNA sequence motif.}

\item{positive_threshold}{The numeric probability threshold (0-1) to classify
a site as "Positive". Defaults to 0.5.}
}
\value{
A `data.frame` with one row and two columns:
  `predicted_m6A_prob` and `predicted_m6A_status`.
}
\description{
A wrapper function for making a prediction on a single candidate site by
providing its features as individual arguments. This is particularly useful
for interactive applications (e.g., Shiny apps) or for quick tests.
}
\examples{
# This is a conceptual example, as it requires a pre-trained model object.
\dontrun{
# Load your pre-trained model
my_rf_model <- readRDS("path/to/your/rf_fit.rds")

# Predict a single site
single_result <- prediction_single(
  ml_fit = my_rf_model,
  gc_content = 0.5,
  RNA_type = "mRNA",
  RNA_region = "CDS",
  exon_length = 12,
  distance_to_junction = 3,
  evolutionary_conservation = 0.9,
  DNA_5mer = "AGACU"
)

print(single_result)
}
}
